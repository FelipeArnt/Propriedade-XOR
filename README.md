# Propriedade-XOR

Repositório de códigos utilizando a propriedade XOR.


- Commutative: A ^ B = B ^ A.
- Associative: (A ^ B) ^ C = A ^ (B ^ C).
- Identity: A ^ 0 --> xor 0 retorna o valor original.
- Self-inverse: A ^ A = 0 --> xor de um valor nele mesmo retorna 0.
